{{> header }}

    <h1>{{ title }}</h1>
    <p>Welcome to {{ title }}</p>

    <ul>
        {{#content.recent}}
            <li>{{ post.title }}</li>
        {{/content.recent}}
    </ul>

{{> footer }}